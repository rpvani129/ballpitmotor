import Link from "next/link";
import { notFound } from "next/navigation";
import { createClient } from "@/lib/supabase/server";
import ConfigurationForm from "../../../ConfigurationForm";
export default async function NewConfigurationPage({ params, searchParams }: { params: Promise<{id:string}>; searchParams: Promise<Record<string,string>> }) { const {id}=await params; const query=await searchParams; const supabase=await createClient(); const {data:track}=await supabase.from("tracks").select("id,name").eq("id",id).single(); if(!track) notFound(); return <main className="dashboard-main"><Link className="back-link" href={`/dashboard/tracks/${id}`}>← {track.name}</Link><section className="page-title compact-title"><p className="eyebrow">NEW LAYOUT</p><h1>Add configuration</h1><p>Create the layout and return to the configuration table.</p></section>{query.error&&<p className="alert">That configuration could not be saved.</p>}<ConfigurationForm trackId={id} trackName={track.name} /></main>; }
